# nube-rubix-updater-cli

```
scp -P22  main pi@192.168.15.100:/home/pi
```


```
// long name
go run main.go mk-build-dir --user=aidan
// short name
go run main.go mk-build-dir -u=aidan

// install bios
./nube-cli-linux-arm install-bios -d /home/pi/bin/builds -t ghp_j9mu
./download-bios-linux-arm install-bios -d /home/aidan -t ghp_j9mu
```

